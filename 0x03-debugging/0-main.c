#include "main.h"
/**
 * main - tests function to print a positive or negative integer
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	i = 0;
	positive_or_negative(i);

	return (0);
}
