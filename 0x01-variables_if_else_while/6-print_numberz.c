#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: Always 0 if success
 */
int main(void)
{
	int m;

	for (m = 48; m <= 57; m++)
	{
		putchar(m);
	}

	putchar('\n');

	return (0);
}
