#include <stdio.h>

/**
 * main - This is the entry point of the program
 *
 * Return 0 if success
 */
int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
	{
		printf("%i", k);
	}

	printf("\n");

	return (0);
}
