#include <stdio.h>

/**
 * main - Entry point to main
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int k;

	for (k = 0; k < 10; k++)
	{
		printf("%i", k);
	}

	printf("\n");

	return (0);
}
